import React, { Fragment } from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <h1>Yellow Castle</h1>
  </React.StrictMode>,
  document.getElementById('app'),
);
